# Интеграция авторизации через VK ID в веб-приложение

## Введение

Кратко о SuperAppKit и сценариях использования.

## Установка

Для интеграции авторизации через VK ID в веб-приложение выполните установку 

## Настройка приложения в VK ID (если требуется)

Ссылка на портал разработчиков, получение ID.

## Сценарии авторизации

### 1. Классическая авторизация с редиректом (Connect.redirectAuth)

- Когда использовать.
- Параметры (url, state, screen, action).
- **Важно:** требование к https.
- Обработка результата: извлечение payload из query-строки.
- Пример кода.

### 2. Быстрая авторизация (OneTap)

- Общее описание.

#### 2.1. Плавающее окно (Floating)

- `Connect.floatingOneTapAuth`
- Пример настройки колбэков.
- Уничтожение окна (destroy).

#### 2.2. Кнопка для вставки (Button)

- `Connect.buttonOneTapAuth`
- Параметр `container` (как привязать к DOM).
- Настройка внешнего вида (displayMode, buttonSkin, langId).
- Пример обработки событий (`LOGIN_SUCCESS`, `FULL_AUTH_NEEDED`).

### 3. Работа с полученными данными

- Структура `VKSilentAuthPayload`.
- Как отправить токен на бэкенд и получить сессию.
- (Опционально) Обновление токена (`AuthAction`).

## Обработка ошибок

- Основные типы ошибок (`ConnectAuthError`).
- Рекомендации по пользовательскому интерфейсу (не просто alert).

## Политика данных

- `Connect.userDataPolicy`

## API Reference (кратко)

Ссылка на полный список типов и методов.
